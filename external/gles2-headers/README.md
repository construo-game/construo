# Khronos OpenGL ES 2.0 headers

Vendored for platforms (notably MinGW/Windows) where `SDL_opengles2.h` includes
`<GLES2/gl2.h>` but the host does not ship those system headers.

Source: Khronos OpenGL-Registry / EGL-Registry (Apache-2.0 / MIT style licenses
as published by Khronos).
