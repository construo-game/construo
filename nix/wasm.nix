# Scaffolding for Emscripten / WASM builds of Construo.
# Adapt from pingus/nix/wasm.nix once the SDL2 + GLES2 backend is validated.
{ pkgs }:
{
  # TODO: emscripten stdenv, SDL2 for wasm32-emscripten, preload examples/,
  # shell.html, and flake output .#construo-wasm
}
